{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/localtunnel-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/localtunnel-server",
        "main": "packages/localtunnel-server/src/cli.ts",
        "tsConfig": "packages/localtunnel-server/tsconfig.lib.json",
        "assets": ["packages/localtunnel-server/*.md"]
      }
    },
    "start": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "localtunnel-server:build",
        "args": ["--port=8089", "--debug", "--domain=localhost"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/localtunnel-server/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/localtunnel-server"],
      "options": {
        "jestConfig": "packages/localtunnel-server/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "conventional",
        "postTargets": ["localtunnel-server:publish", "localtunnel-server:github", "localtunnel-server:docker:production"]
      }
    },
    "github": {
      "executor": "@jscutlery/semver:github",
      "options": {
        "tag": "${tag}",
        "notes": "${notes}"
      }
    },
    "publish": {
      "executor": "ngx-deploy-npm:deploy",
      "options": {
        "access": "public"
      }
    },
    "docker": {
      "executor": "@nx-tools/nx-docker:build",
      "options": {
        "load": true,
        "tags": ["monday/localtunnel-server:latest"]
      },
      "dependsOn": ["build"],
      "configurations": {
        "production": {
          "load": false,
          "push": true,
          "tags": [
            "us-east1-docker.pkg.dev/monday-localtunnel/localtunnel:latest",
            "us-east1-docker.pkg.dev/monday-localtunnel/localtunnel:${version}"
          ]
        }
      }
    }
  },
  "tags": []
}
